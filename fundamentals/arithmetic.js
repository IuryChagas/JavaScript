const [a, b, c, d] = [3, 5, 1, 15];

const sum = a + b + c + d;
const subtraction = d - b;
const multiply = a * b;
const divide = d / a;
const mod = a % 2; // module

console.log(sum, subtraction, multiply, divide, mod);
let dois = 2;
let tres = 3;
let dezoito = 18;

console.log(`\n [2^2]: Math.pow(2,2) = `+ Math.pow(dois,2));
console.log(` [3^2]: Math.pow(3,2) = `+ Math.pow(tres,2));
console.log(`[18^2]: Math.pow(18,2) = `+ Math.pow(dezoito,2));